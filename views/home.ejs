<%- include ("partials/header") -%>


<h1>Home</h1>
<p><%= startingContent  %> </p>


<% posts.forEach(post => { %>
    <h1><%= post.title; %></h1>
    <p><%= post.content.substring(0, 100) %>

        <% if (post.content.length > 101) { %>
            ...
        <% } %>

        <% let link = "http://localhost:3000/posts/" + post.title %> 
        <a href= "/posts/<%= post.title %>">Read More</a>
    </p>
<% }) %>


<%- include ("partials/footer") -%>